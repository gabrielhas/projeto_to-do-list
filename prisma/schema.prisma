generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Tasks {
  task String
  done Boolean
  id   Int     @id @default(autoincrement())
}
